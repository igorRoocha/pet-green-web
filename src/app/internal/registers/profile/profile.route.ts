import { ProfileComponent } from './profile.component';
import { Route } from '@angular/router';

export const profileRegisterRoute: Route = {
    path: 'cadastro-perfil-acesso',
    component: ProfileComponent
}