import { SpeciesComponent } from './species.component';
import { Route } from '@angular/router';

export const speciesRegisterRoute: Route = {
    path: 'cadastro-especie',
    component: SpeciesComponent
}